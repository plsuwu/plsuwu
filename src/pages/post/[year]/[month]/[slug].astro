---
import type { GetStaticPaths } from "astro";
import { render } from "astro:content";
import { getProcessedPosts } from "$scripts";
import PostContent from "$layouts/PostContent.astro";
import ImageModal from "$components/media/ImageModal.astro";

export const getStaticPaths = (async () => {
	const posts = await getProcessedPosts();
	return posts.map((post) => ({
		params: {
			year: post.params.year,
			month: post.params.month,
			slug: post.params.day,
		},
		props: post,
	}));
}) satisfies GetStaticPaths;

const post = Astro.props;
const { Content, remarkPluginFrontmatter } = await render(post);
---

<script>
    
</script>

<PostContent {...post.data}>
	<!-- <aside> -->
	<!--        <div class="text-lg underline">Contents</div> -->
	<!-- 	<ul> -->
	<!-- 		{ -->
	<!-- 			remarkPluginFrontmatter.tableOfContents.map((chapter) => ( -->
	<!-- 				<li style={{ marginLeft: `${(chapter.depth - 2) * 1}em` }}> -->
	<!-- 					<a href={chapter.href}>{chapter.title}</a> -->
	<!-- 				</li> -->
	<!-- 			)) -->
	<!-- 		} -->
	<!-- 	</ul> -->
	<!-- </aside> -->
	<Content />
	<ImageModal target=".prose" />
</PostContent>
